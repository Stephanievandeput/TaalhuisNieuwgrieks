<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.taalhuisnieuwgrieks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LessonOneActivity" />
        <activity android:name=".LessonTwoActivity" />
        <activity android:name=".LessonThreeActivity" />
        <activity android:name=".LessonFourActivity" />
        <activity android:name=".LessonFiveActivity" />
        <activity android:name=".LessonSixActivity" />
        <activity android:name=".LessonSevenActivity" />
        <activity android:name=".LessonEightActivity" />
        <activity android:name=".LessonNineActivity" />
        <activity android:name=".LessonTenActivity" />
        <activity android:name=".LessonElevenActivity" />
        <activity android:name=".LessonTwelveActivity"></activity>
    </application>

</manifest>